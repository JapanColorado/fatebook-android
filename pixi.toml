[workspace]
name = "fatebook-android"
version = "0.1.0"
description = "Fatebook prediction-tracking Android app"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
gradle = ">=9.3"
openjdk = ">=21,<22"

[tasks]
build = { cmd = "./gradlew assembleDebug", env = { ANDROID_HOME = "/usr/lib/android-sdk" } }
install = "adb install -r app/build/outputs/apk/debug/app-debug.apk"
deploy = { depends-on = ["build", "install"] }
run = "adb shell am start -n dev.russell.fatebook/.MainActivity"
clean = { cmd = "./gradlew clean", env = { ANDROID_HOME = "/usr/lib/android-sdk" } }
test = { cmd = "./gradlew test", env = { ANDROID_HOME = "/usr/lib/android-sdk" } }
test-unit = { cmd = "./gradlew testDebugUnitTest", env = { ANDROID_HOME = "/usr/lib/android-sdk" } }
test-screenshot-record = { cmd = "./gradlew recordPaparazziDebug", env = { ANDROID_HOME = "/usr/lib/android-sdk" } }
test-screenshot-verify = { cmd = "./gradlew verifyPaparazziDebug", env = { ANDROID_HOME = "/usr/lib/android-sdk" } }
test-all = { cmd = "./gradlew testDebugUnitTest verifyPaparazziDebug", env = { ANDROID_HOME = "/usr/lib/android-sdk" } }
lint = { cmd = "./gradlew lintDebug", env = { ANDROID_HOME = "/usr/lib/android-sdk" } }
