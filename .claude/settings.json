{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(echo \"$TOOL_INPUT\" | python3 -c \"import sys,json; print(json.load(sys.stdin).get('file_path',''))\" 2>/dev/null); if echo \"$file\" | grep -q '\\.kt$'; then cd /home/russell/Projects/FatebookApp && pixi run lint 2>&1 | tail -20; fi"
          }
        ]
      }
    ]
  }
}
